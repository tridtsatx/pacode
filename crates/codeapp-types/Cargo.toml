[package]
name = "codeapp-types"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pure data contracts shared by every codeapp crate: ids, messages, protocol, state DTOs, config"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
pretty_assertions = { workspace = true }
